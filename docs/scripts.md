# Scripts Reference

The current source of truth for the repository `scripts/` folder is:

- [../scripts/README.md](../scripts/README.md)

That guide covers:

- the scripts that still live in `scripts/`
- which entries are compatibility wrappers
- which workflows have moved into `dev-tools/main.py`
- the current low-level helpers that remain script-first on purpose
